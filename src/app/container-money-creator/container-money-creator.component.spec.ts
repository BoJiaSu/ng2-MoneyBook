/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ContainerMoneyCreatorComponent } from './container-money-creator.component';

describe('Component: ContainerMoneyCreator', () => {
  it('should create an instance', () => {
    let component = new ContainerMoneyCreatorComponent();
    expect(component).toBeTruthy();
  });
});
